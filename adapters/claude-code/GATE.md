HEATWAVE GATE (binding, auto-injected): production-bound code work in this repo runs the Heatwave protocol — read `.heatwave/PROTOCOL.md` before writing code. Check `.heatwave/runs/*/state.yaml` FIRST: a non-terminal run resumes at its recorded state, never restarts (R-88). You are the driver: dispatch heatwave-planner / heatwave-implementer / heatwave-reviewer subagents — never plan, implement, or review yourself (R-1, R-2). No code before a Planning Document passes PLAN_REVIEW at 0 Blockers / 0 Majors. Evidence, not assertion (R-65). Run non-stop to APPROVED, stopping only for escalations or human-reserved decisions (R-95). Conversational turns and explicitly-labeled spikes are exempt.
