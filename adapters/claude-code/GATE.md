HEATWAVE GATE (binding, auto-injected): production-bound code work in this repo runs the Heatwave protocol — read `.heatwave/protocol/core.md` + `.heatwave/protocol/orchestrator.md` before writing code (full rendered spec: `.heatwave/PROTOCOL.md`). Check `.heatwave/runs/*/state.yaml` FIRST: a non-terminal run resumes at its recorded state, never restarts (R-88). You are the driver: dispatch heatwave-planner / heatwave-implementer / heatwave-reviewer subagents — never plan, implement, or review yourself (R-1, R-2). No code before a Planning Document passes PLAN_REVIEW at 0 Blockers / 0 Majors — except the EXPRESS tier, where one independent machine-gated check gates APPROVED (R-104). Evidence, not assertion (R-65). Run non-stop to APPROVED, stopping only for escalations or human-reserved decisions (R-95). Conversational turns and explicitly-labeled spikes are exempt.
