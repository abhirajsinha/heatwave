# Implementation Package

task_id: hw-v4-B-machine-evidence | artifact_type: implementation-package | iteration: 1 | produced_by: IMPLEMENTER (claude-fable-5) | timestamp: 2026-08-11

Branch: `heatwave-v4-subproject-b` (local only, on top of `main` @ `0cecd88`). Plan: `docs/superpowers/plans/2026-08-10-machine-evidence-rigor.md` (iteration 2, PLAN_REVIEW passed).

## Change Summary

Sub-project B (Machine-Evidence Rigor) built exactly to the approved plan, T1–T11 in order: tier-rigor table + R-110/R-114/R-115 in core.md; §3.4.2 (R-111 machine findings, R-112 refute-or-promote) + R-113 reviewer half + Appendix A extension (`Origin`, `Refutation`, `Refuted`) + R-109 amendment in reviewer.md; R-113 planner half + §6.1 sast/mutation declaration contract in planner.md; R-113 implementer half in implementer.md; driver recording duties in orchestrator.md; ledger/run-record schema extensions (all optional + defaulted); planning-document/review-report template lines; commented config keys; four prompt updates; history F.1 row. PLAN_REVIEW residual F-hwB-011 folded in (R-31 refuted carve-out in fixer.md). PROTOCOL.md regenerated after every shard task; drift check green throughout. Live battery run on scratch targets with declared stub tools (T11): all four tiers exercised, both refute-or-promote directions observed, red-then-green enforced both ways, hetero advisory recorded in both polarities.

## Files Changed

| Path | Change type | Task |
|---|---|---|
| protocol/core.md | extended (tier-rigor table, R-110, R-115, R-114, change_class in §2.5, four-field drive-behavior sentence, R-77 Refuted exclusion) | T1 |
| protocol/reviewer.md | extended (§3.4.2 R-111/R-112, ladder-first sentence + R-113 reviewer half in §4.4, Appendix A rows/enum, R-109 final sentence) | T2 |
| protocol/fixer.md | one clause appended to R-31 (refuted findings need no response) | T2 (F-hwB-011 residual, directed by PLAN_REVIEW) |
| protocol/planner.md | extended (R-113 planner half after R-27; §6.1 v4 paragraph after R-99; two example rows) | T3 |
| protocol/implementer.md | extended (R-113 implementer half after R-38; §3.3 Test-results row) | T4 |
| protocol/orchestrator.md | extended (§9.1 driver-duties paragraph after R-85; §9.2 read-only confirmed, no edit) | T5 |
| templates/findings-ledger.yaml | machine_evidence block; origin/refutation comment rows; status enum + refuted | T6 |
| templates/run-record.yaml | run_config.change_class; top-level hetero_reviewer | T6 |
| templates/planning-document.md | Change-class line; bugfix repro AC guidance; SAST/Mutation tooling rows | T7 |
| templates/review-report.md | Machine-evidence line atop Verification Log | T7 |
| heatwave.config.example.yaml | R-115 recommendation comment; commented sast/mutation keys | T8 |
| prompts/reviewer.md | ladder-first block; refute-or-promote + bugfix bullets | T9 |
| prompts/planner.md | change-class bullet; sast/mutation detection sentence | T9 |
| prompts/implementer.md | red-first evidence bullet | T9 |
| prompts/orchestrator.md | intake step 4 (change_class); hetero advisory sentence in loop step 2 | T9 |
| protocol/history.md | one F.1 row (R-110–R-115) | T10 |
| PROTOCOL.md | regenerated by build-protocol.sh after every shard task | T1–T5, T10 |

No other file touched. `git diff --name-only main..HEAD` output (17 files, all pre-existing .md/.yaml) attached under AC-N-01 below.

## Diff

Branch `heatwave-v4-subproject-b`, commits (oldest first): `ee55776` T1, `05e8499` T2(+F-hwB-011), `3d43441` T3, `906f7e7` T4, `ad9f9e1` T5, `6885a0f` T6, `1ab91d5` T7, `656caab` T8, `8cc8694` T9, `1d8ea1e` T10, plus the T11/docs commit containing this package.

## Deviation Records

None self-approved. Three execution notes flagged for the REVIEWER to disposition (all verification-harness variances, none touching the shipped rule text, which is verbatim from the plan):

1. **L4 executed as a controlled dispatch, not a full live FULL run.** The plan's Risks table designates "controlled reviewer-only dispatches as fallback" for live-run nondeterminism. L4's PLANNING→IMPLEMENTING artifacts were seeded fixtures (clearly crafted, R-3-legal); FULL_REVIEW and FIXING were live dispatches. Every L4 AC check (machine mutation finding, promote-with-refutation, refuted finding excluded from FIXING, fix report carrying the open Blocker) was produced by live role contexts against those fixtures.
   - What the plan specified: L4 as a live run ("Run a small feature") with seeded defect.
   - What was done instead: seeded plan/package fixtures + live FULL_REVIEW + live FIXING.
   - Why: determinism; the plan's own fallback mechanism.
   - Affects review scope / ACs: none — AC-F-02/03 verified on the produced artifacts. Affects threat surface: none.
2. **N3 skipped per the plan's stated skip condition.** The plan says N3 is skipped if a refuted Major+ arises organically. Two arose: L4's machine mutation finding (F-l4-report-002, `status: refuted`, refuted by re-run + mutant application + attribution check) and N2's exit-status Major candidate (refuted by execution). Both are absent from all fix cycles.
3. **Two plan check-commands were mis-calibrated against their own edits; content matches the plan exactly, commands re-anchored.** (a) T2's `sed -n '/Appendix A/,$p'` grep expected 3 — anchored at R-29's earlier "Appendix A" mention it counts 5 because the plan's own new R-109/R-112 sentences match; anchored at the `## Appendix A` heading it returns exactly 3. (b) T3's `grep -n "sast" | wc -l` expected ≥2 — the plan's verbatim rule text puts both lowercase mentions on one line and the example row is uppercase `SAST`; case-insensitive grep shows both required sites (rule text line 106, example row line 96). Related: F-hwB-012 (Nit) — the plan's F-hwB-005 response block contains an inaccurate grep claim; it is NOT echoed into any committed artifact (verified: the claim exists only inside the plan document's response block; no shard/template/prompt carries it).

## Migration Notes

None — no migrations. Pre-B artifacts read under defaults: absent `change_class` = `feature`, absent `hetero_reviewer` = pre-B/not-yet-computed, absent `machine_evidence` = pre-v4-B ledger. Verified live (AC-N-03 below).

## Configuration Changes

None active. `heatwave.config.example.yaml` gains commented `tooling.sast` / `tooling.mutation` examples and an R-115 recommendation comment — zero-config behavior unchanged.

## Test Additions

No repo tests exist (docs/templates repo). Verification is the plan's two layers: deterministic self-checks per task (grep assertions + drift check, all run and attached) and the T11 live battery on scratch targets under `/private/tmp/hw-b-verify/` (evidence bundle preserved there: l1..l4, n1, n2, h1, resume-check, plus record snapshots).

## Test Results

### Drift check (G-5, run after every shard task; final tree)

```
$ sh build-protocol.sh && sh build-protocol.sh --check
generated PROTOCOL.md from protocol/ shards
OK: PROTOCOL.md matches protocol/ shards
(exit 0)
```

### AC-F-01 — ladder pre-LLM; absent tool → explicit NOT_AVAILABLE

(a) L3 ledger machine_evidence (STANDARD), recorded before findings; both rungs REVIEWER-executed:
```
machine_evidence:
  - rung: tests   verdict: pass  (REVIEWER ran sh test.sh)
  - rung: sast    verdict: pass  (REVIEWER ran sh ./sast-stub.sh)
```
(b) N2 (sast undeclared) ledger entry — explicit degradation, never silent:
```
  - rung: sast
    tool: "none declared (heatwave.config.yaml tooling.sast unset); probed semgrep/shellcheck/codeql/bandit via PATH, ~/.local/bin, pip3, brew — none present"
    verdict: NOT_AVAILABLE
    evidence_ref: "probe output: 'semgrep not found / shellcheck not found / codeql not found / bandit not found'; second probe empty"
    unverified_acs: []   # AC-F-01/AC-F-02 verified by the tests rung ... Gap (R-64) stated + compensating manual read logged
```
**Honest variance:** AC-F-01b's wording expects non-empty `unverified_acs`; in this fixture no declared AC depended on SAST, so the honest value is `[]` with the R-64 gap narrated in the entry. The degradation contract (explicit NOT_AVAILABLE + evidence + gap statement) is fully exercised. Reviewer to confirm this satisfies the criterion's intent.
Transcript-order caveat (R-64): subagent internal transcripts are not attachable; ordering evidence is the ledger's machine_evidence-before-findings structure plus each report's verification log stating rungs ran first.

### AC-F-02 — mutation finding on FULL

L4 ledger F-l4-report-002: `origin: machine`, `rung: mutation`, problem contains "(tests inadequate for report.sh)", default Major. Present as required; refuted with recorded evidence (see AC-F-03).

### AC-F-03 — refute-or-promote, both directions

(a) Refuted: F-l4-report-002 `status: refuted` — refutation re-ran the rung, applied the named mutant to a scratch copy (`avg=`→`avG=`), test suite killed it (`FAIL avg`, exit 1), and noted the tool cited a nonexistent line — not attributable to the change. Absent from 05-fix-report.md as a response item (fixer cited the R-31 carve-out verbatim). Second organic refutation: N2's exit-status Major candidate, refuted by execution (`echo $?` → 0 both paths).
(b) Promoted: F-l4-report-001 (machine Blocker, failing declared test) — refutation attempt recorded ("re-ran; baseline HEAD^ PASS → attributable; not refutable — promoted"), `status: open`, entered FIXING, fixed in commit `ad0eddd`, `sh test.sh` → `PASS`, exit 0.
(c) Schema sweep across all six battery ledgers: every `severity: Blocker|Major` entry has non-empty `refutation` (awk sweep output: 4 "refutation present", 0 "EMPTY REFUTATION"; L2/L3 ledgers contain no Major+ findings — vacuous there).

### AC-F-04 — reproduce-then-fix, both directions

(a) L2 Implementation Package carries the pair, captured red-first on unmodified code:
```
FAIL: expected 6, got 3      (pre-fix, exit 1)
PASS: sum 1..3 = 6           (post-fix, exit 0)
```
(b) N1 (package with no red evidence): reviewer raised exactly one open Major — F-n1-fix-001, `category: verification-integrity`, problem "no red output from the reproduction check executed on pre-fix code is attached (R-113 implementer half...)" — with a three-way refutation attempt recorded (searched package/run-dir; checked VCS history; reconstructed pre-fix code to prove a red run was obtainable). GATE_NOT_MET.

### AC-F-05 — hetero-reviewer visibility, computed when both roles resolved

(a) L2 final run-record: `hetero_reviewer: "false (self-preference bias not mitigated)"`. Timing (append-order proof): the field is absent from both pre-review snapshots (`l2-record-at-planreview.snapshot`, `l2-record-pre-review.snapshot` — grep count 0 in each) and in the final record it appears AFTER the `IMPLEMENTING → FULL_REVIEW` transition entry; diff of pre-review snapshot vs final shows only the impl transition, the hetero line, and the APPROVED transition appended.
(b) H1 (different-model reviewer, live Opus dispatch — model switching WAS available, no R-64 degradation needed): run-record has `roles.implementer.resolved: claude-fable-5` vs `roles.reviewer.resolved: claude-opus-4-8` and `hetero_reviewer: "true"`; both review artifacts carry `produced_by: REVIEWER (claude-opus-4-8)`; the Opus reviewer ran the LIGHT tests rung itself and returned GATE_MET (0/0, 1 Nit).

### AC-F-06 — tier scaling (grep across battery run dirs)

```
L1 (EXPRESS):  0 rung/machine_evidence/ladder mentions anywhere in the run dir
L2 (LIGHT):    rungs = { tests }
L3 (STANDARD): rungs = { tests, sast }
L4 (FULL):     rungs = { tests, sast, mutation }
```

### AC-F-07 — regression + drift

L1 EXPRESS → APPROVED with exactly the two-artifact pipeline (`01-express-change.md`, `02-express-check.md`); README fixed. L3 → APPROVED through the full state machine (01-plan → 02-plan-review → 03-package → 04-full-review pair → 05-final-review pair), FINAL_REVIEW re-ran the tests rung and carried the sast verdict forward by reference with the prior `evidence_ref` named and diff-empty eligibility verified (D-7 exercised live). Final tree drift check: `OK: PROTOCOL.md matches protocol/ shards`, exit 0.

### AC-F-08 — adapter/prompt consistency (T10 sweeps, verbatim results)

- `grep -rniE "ladder|refut|mutation|sast|change_class|hetero" adapters/ README.md COMPANIONS.md docs/faq.md install.sh` → 0 hits. Same pattern over `prompts/` hits only the four files T9 edited.
- `grep -rn "evidence, not assertion|reviewer owns severity" adapters/` → 8 adapter shims, each carrying only role/tier/evidence invariants; zero contradictions; zero adapter edits.
- Rule uniqueness (check 4): `grep -rEn "R-11[0-5]\.|R-113 \((planner|implementer|reviewer) half\)\." protocol/` → R-110, R-111, R-112, R-114, R-115 once each; R-113 exactly three named halves.
- Status-enum stragglers (check 5): `grep -rn "Deferred (approved)" protocol/ templates/` → 3 hits: Appendix A (enum continues `Disputed | Refuted`), core.md R-77 (lists `Refuted`), core.md R-6 (deferral prose, correctly unchanged). Ledger template uses `deferred_approved ... | refuted`.

### AC-F-09 — rules present, schema consistent, PROTOCOL.md regenerated

```
for r in 110 111 112 114 115: grep -c "R-$r\." PROTOCOL.md → 1 1 1 1 1
grep -c "R-113 (" PROTOCOL.md → 3
sed -n '/## Appendix A/,$p' PROTOCOL.md | grep -c "Refuted" → 1
head -1 PROTOCOL.md → "<!-- GENERATED FILE — do not edit. ... -->"
```

### AC-N-01 — zero new runtime deps

`git diff --name-only main..HEAD` → 17 files, all pre-existing `.md`/`.yaml` (PROTOCOL.md, heatwave.config.example.yaml, 4 prompts, 7 protocol shards, 4 templates) plus `docs/` trail. `git diff --quiet main..HEAD -- build-protocol.sh` → BUILD-SCRIPT-UNTOUCHED. `git diff main..HEAD --summary | grep -E "mode|create"` → no new files, no mode changes.

### AC-N-02 — EXPRESS unchanged

`git diff --quiet main..HEAD -- prompts/express-checker.md templates/express-change.md templates/express-check.md` → exit 0 (EXPRESS-SURFACE-UNTOUCHED). L1 run produced the identical A-era artifact set (two artifacts, APPROVED, zero ladder text).

### AC-N-03 — resume compatibility

Copy of L3's mid-flight run dir with every v4-B field stripped (grep count of `change_class|hetero_reviewer` = 0), resumed by the driver under §2.5 defaults: diff of record before/after resume shows exactly one appended transition line, nothing rewritten:
```
21a22
>   - { from: FULL_REVIEW, to: FINAL_REVIEW, artifact: ..., timestamp: ... }
```

## Blast Radius Declaration

Touched: 7 protocol shards, 4 templates, 4 role prompts, the example config, the generated PROTOCOL.md. Consumers: every future Heatwave dispatch in any adapted repo (shards/prompts/templates are the dispatch surface); `install.sh` copies them verbatim (no installer change needed — verified it copies whole directories). Shared state/schema: findings-ledger and run-record schemas extended optionally (defaults defined in the schema comments and §2.5); `state.yaml` untouched; §2.1/§2.2 states untouched. Contracts: EXPRESS surface byte-identical; fixer contract extended by one R-31 clause; adapter shims unchanged (sweep-verified compatible). Reasoning: B is rule-text + schema only; the only executable in the repo (`build-protocol.sh`) is byte-identical, so the blast radius is exactly the documents changed plus their future readers.

## Known Limitations

- Real SAST/mutation tools are NOT AVAILABLE in this environment (`command -v semgrep mutmut stryker` empty); the tool-agnostic contract was exercised with declared stub tools per the plan. Wiring real tools is sub-project D.
- L4's TARGETED/FINAL states were not run (AC checks end at the Fix Report); N3 skipped per plan condition; L4 upstream artifacts were fixtures (Deviation notes 1–2).
- Subagent internal transcripts are not attachable as evidence; rung-before-findings ordering rests on ledger structure + report verification logs (noted under AC-F-01).
- No `ponytail:` ceiling comments — no code was written.

## Tooling Status

Per §6.2: semgrep/CodeQL (SAST) and stryker/mutmut/cargo-mutants (mutation) unavailable this session — would have verified nothing here beyond what stubs verify, since B's ACs test the recording/degradation contract, not scan quality; no AC left unverified by their absence. Claude Code adapter + subagent dispatch available and used for the whole battery (12 live role dispatches). Different-model reviewer dispatch (H1): AVAILABLE and used — the plan's "assumed" capability verified live (Opus 4.8 reviewer context; artifacts self-identify `claude-opus-4-8`). The plan's R-64 degradation path for AC-F-05b was not needed.
