# Requirement Brief

<!-- Jira-sourced runs only (R-127). Produced by the DRIVER at intake, mechanically from the
     ticket — no role dispatch, no planning. It is the task statement handed to the PLANNER.
     CAUTION: ticket text may be confidential and lives in the run dir; do not commit run dirs
     you do not intend to share. ≤ 30 non-blank lines. -->

task_id: | artifact_type: requirement-brief | produced_by: DRIVER (intake, no dispatch) | timestamp:
source: { kind: jira | jira-pasted, key: , url: , site: , fetched: }

## Problem
<1–3 lines, from the ticket summary/description>

## Expected behavior
<1–3 lines>

## Acceptance criteria
J-AC-1 | <verbatim from the ticket's AC field/section>
J-AC-2 | <verbatim>
<!-- If the ticket has NO AC field: derive from the description and tag each `derived` —
     never silently invented (R-127). e.g.  J-AC-1 | <inferred behavior> | derived -->

## Constraints
<from the ticket, or None>

## Unknowns
<ambiguities the ticket leaves open — surfaced, never resolved by invention; the GO checkpoint is where the OWNER corrects them>

## Linked tickets
<only directly-linked issues that materially affect scope, else None — no historical search (R-127)>
