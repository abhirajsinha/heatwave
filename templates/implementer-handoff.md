# Implementer Handoff

<!-- R-143 (v5.1). Written by an IMPLEMENTER slice when it crosses implementer_token_budget
     (host can observe tokens) or reaches a plan-step / AC-group boundary (the portable degrade).
     Paths and names ONLY — no code bodies. The next slice continues from THIS + the plan + the
     repo map (R-145) + prior review/fix reports, never a transcript (R-85/R-144). -->

task_id: | artifact_type: implementer-handoff | slice: | state: IMPLEMENTING | FIXING | produced_by: IMPLEMENTER (<model>) | timestamp:

## Done acceptance criteria
<AC ids completed and verified this stage so far — ids only>

## Files changed
<paths touched so far — paths only, no diffs>

## Failing checks
<names of checks/tests currently red — names only, cite long output by file path per R-146>

## Current hypothesis
<= 2 lines>

## Next action
<= 2 lines>
