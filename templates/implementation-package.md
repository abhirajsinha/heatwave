# Implementation Package

task_id: | artifact_type: implementation-package | iteration: | produced_by: IMPLEMENTER (<model>) | timestamp:

## Change Summary
<what was built, prose, ≤ 200 words>

## Files Changed

| Path | Change type | Line delta |
|---|---|---|

## Diff
<inline, or a reference the REVIEWER can resolve (branch, commit range)>

## Deviation Records
<per §3.2.1, one block per deviation, or explicit `None`>

- What the plan specified:
- What was built instead:
- Why:
- Affects review scope / acceptance criteria / non-functional targets:
- Affects threat surface:

## Migration Notes
<forward and backward, or `None`>

## Configuration Changes
<new env vars, flags, secrets, or `None`>

## Test Additions
<what was added and what it covers>

## Test Results
<real command output / logs / artifacts per §6.4 — evidence, not assertion>

## Blast Radius Declaration
<per §5.4: components touched, their consumers, shared state/schema, contracts, reasoning for the boundary — or explicit `None` with reasoning>

## Known Limitations
<including every `ponytail:` ceiling comment (R-93), or explicit `None`>

## Tooling Status
<per §6.2: any unavailable tool, what it would have verified, which ACs are consequently unverified>
