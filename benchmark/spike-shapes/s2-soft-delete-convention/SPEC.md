# Task: implement count_users()

Implement `count_users(store)` in `metrics.py` (keep the file and function
name). It returns the number of users in the store as an integer.

`store` is a `Store` (see `store.py`). A basic check exists in
`test_visible.py`.
